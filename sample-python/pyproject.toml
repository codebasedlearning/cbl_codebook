[project]
name = "sample-python"
version = "0.9.0"
description = "Python samples for the Codebook plugin: didactic prose in dash-framed docstrings."
readme = "README.md"
requires-python = ">=3.11"

dependencies = []

[tool.uv]
package = false

[tool.ruff]
line-length = 100
target-version = "py311"
